services:
  ha-tablet-dash:
    build: .
    ports:
      - "4567:4567"
    volumes:
      - ./config:/config
      - ./frontend:/app/frontend
    environment:
      - HA_URL=http://homeassistant.local:8123
      - HA_TOKEN=your_long_lived_token_here
