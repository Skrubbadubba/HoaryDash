{{define "navbar"}}
<nav class="navbar">
    {{range $i, $screen := .}}
    <div class="navbar-item{{if eq $i 0}} active{{end}}" onclick="window.navigateTo({{$i}})">
        <div class="navbar-icon">
            {{if $screen.Icon}}
                {{icon $screen.Icon}}
            {{else}}
                {{icon "circle-medium"}}
            {{end}}
        </div>
        <div class="navbar-label">{{$screen.Name}}</div>
    </div>
    {{end}}
</nav>
<script>
component(function(navbar) {
    // Needed because absolute positioned elements are apprantly
    // not layed until a second paint
    requestAnimationFrame(function() {normalizeWidth(navbar)});

    var items = navbar.querySelectorAll('.navbar-item');
    window.addEventListener('screen-navigate', function(e) {
        console.log("screen navigated event! ", e)
        console.log("looping through navbar items: ", items)
        for (var i = 0; i < items.length; i++) {
            console.log("loop in navbar items: ", i, items[i])
            if (i === e.detail.index) {
                items[i].className = 'navbar-item active';
            } else {
                items[i].className = 'navbar-item';
            }
        }
    })
});
</script>
{{end}}